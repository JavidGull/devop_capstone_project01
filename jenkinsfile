pipeline
{

agent any 


environment{
    NAME = "javid"
}
tools {
  maven 'mymaven'
}

stages{

    stage('deploy_dev')
    {
      steps
        {
          dir("data"){
            sh 'sudo docker build -f ./dockerfile -t caps_project01'    
           } 
        }
    }

  } 
}